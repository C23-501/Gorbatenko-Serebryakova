1) Спросить завтра про адреса:
    У нас имеется 256 колонок по 16 слов в каждой колонке. Если нам приходит адрес колонки равный 253 (к примеру) то при burst = 8
    получается надо писать 253, 254, 255, ??? (следующая строка? Вообще берст прервать нельзя) Поэтому для берста адреса должны
    быть выровнены по burst (col mod burst).




    Задачи

1) Адреса
2) Загрузка данных
3) Выгрузка данных
3) Загрузка маски
